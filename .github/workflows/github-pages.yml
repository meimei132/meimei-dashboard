name: 部署到 GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

# 暂时禁用，使用 Vercel 部署
# permissions:
#   contents: read
#   pages: write
#   id-token: write

# concurrency:
#   group: "pages"
#   cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 跳过部署
        run: echo "使用 Vercel 部署，跳过 GitHub Pages"
